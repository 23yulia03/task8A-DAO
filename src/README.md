# Task8A-DAO - Управление продуктами и тегами

## Описание проекта

Проект представляет собой **JavaFX-приложение** для управления списком продуктов и их тегов (категорий).  
Продукты и теги хранятся в **оперативной памяти** в виде коллекций (`ObservableList`) внутри класса `ListProductDAO`.  
Данные существуют **только во время работы приложения** и не сохраняются после его завершения.

## Основные функции

**Добавление продукта** – пользователь может добавить новый продукт, указав его название, количество и тег.  
**Редактирование продукта** – можно изменить название, количество или тег существующего продукта.  
**Удаление продукта** – пользователь может удалить продукт из списка.  
**Добавление тега** – возможность создавать новые категории для товаров.  
**Просмотр списка продуктов** – таблица автоматически обновляется при изменении данных.  
**Просмотр списка тегов** – возможность видеть все доступные категории.

## Структура проекта

- **`HelloController`** – контроллер, управляющий пользовательским интерфейсом. Обрабатывает ввод данных, обновление таблицы и взаимодействие с `ProductDAO`.
- **`ProductDAO`** – интерфейс, определяющий методы для работы с товарами и тегами.
- **`ListProductDAO`** – реализация `ProductDAO`, хранящая данные в `ObservableList`.
- **`Product`** – модель продукта. Хранит информацию о названии, количестве и категории (теге).
- **`Tag`** – модель тега. Хранит идентификатор и название категории продукта.
- **FXML-файл (`.fxml`)** – графический интерфейс, содержащий таблицу, поля ввода и кнопки управления.

## Как использовать

### Добавление продукта
1. Введите название продукта в поле **"Название"**.
2. Введите количество продукта в поле **"Количество"**.
3. Выберите тег из выпадающего списка.
4. Нажмите кнопку **"Добавить"**.

### Редактирование продукта
1. Выберите продукт в таблице.
2. Внесите изменения в поля **"Название"**, **"Количество"** или выберите другой тег.
3. Нажмите кнопку **"Редактировать"**.

### Удаление продукта
1. Выберите продукт в таблице.
2. Нажмите кнопку **"Удалить"**.

### Добавление тега
1. Введите название тега в соответствующее поле.
2. Нажмите кнопку **"Добавить тег"**.

## Поиск и фильтрация

Продукты и теги можно искать по параметрам, таким как:
- **Название продукта**
- **Количество продукта**
- **Категория (тег)**

## Соответствие требованиям

- **Хранение данных в памяти** – данные существуют только во время работы приложения и хранятся в `ObservableList` внутри `ListProductDAO`.
- **Использование DAO** – вся работа с данными осуществляется через `ProductDAO`, что обеспечивает модульность и возможность замены хранилища.
- **Поиск и фильтрация** – таблица позволяет сортировать и фильтровать данные по названию, количеству и тегу.